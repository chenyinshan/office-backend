server:
  port: 8081
spring:
  application:
    name: oa-portal

# 下游服务地址（本地开发时 user、workflow 各起一个实例）
app:
  user-service-url: http://localhost:8082
  workflow-service-url: http://localhost:8083

