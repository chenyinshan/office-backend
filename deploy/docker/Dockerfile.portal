# 需先在项目根目录执行：mvn package -DskipTests
# 构建：docker build -f deploy/docker/Dockerfile.portal -t oa-portal:local .
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY oa-portal/target/oa-portal-*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "app.jar"]
