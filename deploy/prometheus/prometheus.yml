# Prometheus 最小配置，仅抓自身；应用接入后在此添加 scrape_configs
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
