## 自动化运维能力

1. **中间件一键启动**：用 `deploy/docker-compose.yml` 一条命令拉起 MySQL / Redis / MinIO / Nacos，内置健康检查和启动顺序控制，降低本地/测试环境搭建成本。
  中间件一键启动脚本示例（终端输出截图）：
   中间件一键启动脚本示例
2. **应用容器化编排**：通过 `deploy/docker-compose.oa.yml` 和四个 Dockerfile，将 user/workflow/portal/gateway 全部容器化，支持一键构建镜像和启动完整后端栈。
  应用容器化编排脚本示例（终端输出截图）：
   应用容器化编排脚本示例

## 测试与质量保障

1. **核心功能单元测试**：在 `oa-common` 和 `oa-user-service` / `oa-workflow-service` 中编写了若干 JUnit 单元测试（如 `AuthServiceTest`、`GlobalExceptionHandlerTest`、`BizExceptionTest` 等），覆盖统一返回体、全局异常处理以及登录/当前用户、工作流工具类等关键逻辑。

   核心功能单元测试执行示例（终端输出截图）：

   ![核心功能单元测试执行示例](/Users/Ethan/.cursor/projects/Users-Ethan-Desktop-office-backend/assets/core_unit_tests_mvn_test.png)

   核心功能单元测试代码示例（AuthServiceTest 部分代码截图）：

   ![核心功能单元测试代码示例](/Users/Ethan/.cursor/projects/Users-Ethan-Desktop-office-backend/assets/auth_service_test_code.png)

2. **可独立运行的测试模块**：各服务测试代码放在各自模块的 `src/test/java` 下，可通过 `mvn test` 或 `mvn -pl 模块 test` 独立执行，便于在本地或后续 CI 中集成。

   单模块测试执行示例（终端输出截图）：

   ![单模块测试执行示例](/Users/Ethan/.cursor/projects/Users-Ethan-Desktop-office-backend/assets/single_module_tests_oa_user_service.png)

3. **CI/CD 流水线**：根目录 `Jenkinsfile`，包含后端 `mvn test + package`、前端 `npm run build` 以及可选的 Docker 镜像构建阶段，可直接接入 Jenkins 作为多分支流水线的基础脚本。

